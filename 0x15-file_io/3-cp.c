#include "holberton.h"
/**
  * main - main.
  * @argc: count.
  * @argv: value.
  * Return: -1 or 0.
  */
int main(int argc, char *argv[])
{
	if (!argv)
		return (-1);
	if (argc < 1 || argc > 3)
		return (-1);
	return (0);
}
